spring:
  config:
    import: configserver:http://config-server:8888
  application:
    name: gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/

