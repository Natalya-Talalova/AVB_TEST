spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
      failFast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
